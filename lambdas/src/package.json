{
  "name": "http-sns",
  "version": "1.0.0",
  "description": "",
  "main": "./src/function.js",
  "scripts": {
    "build": "sam build -t ../template.yaml -b ../.aws-sam",
    "deploy": "sam deploy -t ../.aws-sam/template.yaml --stack-name lennie-lambda-httpsns --s3-bucket jvb-code --s3-prefix SAM --profile joe --region ap-southeast-2 --capabilities CAPABILITY_IAM"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.987.0"
  }
}
